---
- name: install gpg
  hosts: "{{ host_group }}"
  become: yes
  become_method: sudo
  tasks:
    - name: mise a jour du repos
      shell:
        cmd: "apt update -y"

    - name: install gpg
      apt:
        name: python3-gpg.x86_64
        state: latest

