---
- name: install gpg
  hosts: "{{ host_group }}"
  become: yes
  become_method: sudo
  tasks:
    - name: mise a jour du repos
      shell:
        cmd: "yum update -y"

    - name: install gpg
      yum:
        name: gpg
        state: latest

